FEATURE 1:
python3 main.py --inspect

- Input Signatures: date_to_find (datetime object), securities (int), query_stat (string)
- Output Signature: The relevant metric (query_stat) of a stock (securities) at a certain point in time (date_to_find).

- This command line does not require any terminal argument.
- Upon execution, the Python program will prompt argument on its own to prevent any formatting error. Given the complexity of the required input.

----------------------------

FEATURE 2:
python3 main.py --sector_ROI

- Input Signature: sector (string), prices_df (Pandas dataframe), list_df (Pandas dataframe)
- Output Signature: The calculated return on investment (float data format), from a specified sector.  

- This command line does not require any terminal argument.
- Only the "sector" argument requires manual input. Other aruguments are specified internally by the main.py function.
- Upon execution, the Python program will scan the dataset and return a list of all available market sectors and their numeric identifier.
- The user would then use a number that corresponds with a sector. This is to minimize input error.